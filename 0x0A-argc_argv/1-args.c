#include <stdio.h>

/**
 * main - entry point.
 * @argc: count
 * @argv: arguments string.
 * Return: always 0 on success.
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}
